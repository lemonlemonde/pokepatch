export const metadata = {
  title: "Queue",
  description:
    "Live PokePatch restoration queue — priority and standard lanes. Customer names are hidden.",
};

export default function QueueLayout({ children }) {
  return children;
}
