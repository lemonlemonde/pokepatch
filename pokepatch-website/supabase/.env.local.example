# Optional overrides only. Prefer putting edge secrets in
# pokepatch-website/.env.local.prod — `npm run local` syncs them automatically.
#
# If this file exists as supabase/.env.local, its KEY=value lines win over
# .env.local.prod for the local edge runtime.
